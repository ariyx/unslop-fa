# human-jadi-001 — expected detect baseline

**Expected findings:** none.

This fixture is intentionally human, conversational, and instructional. Do not normalize it merely because it contains surface patterns that can also appear in generated prose.

## Near misses that should remain unflagged

- `در نتیجه` — used once as a real causal connector, not a canned paragraph transition (`C05`).
- `خیلی خیلی زیاده` and repeated `بسیار` — colloquial or evaluative emphasis in the writer's teaching voice, not generic promotional inflation (`P07`).
- `تا حدی` — carries ordinary scope in context and is not part of habitual modal stacking (`P05`).
- Repeated imperatives such as `شروع کنین` / `پیش برین` — instructional rhythm, not semantic repetition (`C01`) or robotic cadence (`C09`).
- The motivational closing — grounded in the article's teaching voice and should not be treated as portable filler or a fake-profound ending (`C07`, `C10`).
