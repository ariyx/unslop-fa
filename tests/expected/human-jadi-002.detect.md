# human-jadi-002 — expected detect baseline

**Expected findings:** none.

This fixture is a strongly voiced personal technical review. Subjective judgments, rhetorical questions, fragments, humor, and uneven cadence are part of the author's style.

## Near misses that should remain unflagged

- Repeated `در نتیجه` — each occurrence expresses a local causal relation; it is not a templated transition pattern (`C05`).
- `بسیار خوب`, `بسیار محدود`, `شدیدا وابسته` — first-person review judgments, not generic marketing intensifiers (`P07`).
- `در واقع` and `یعنی` — explanatory uses that add or clarify a concrete point rather than merely restating it (`C02`).
- `نه اینکه ...` — a genuine contrast inside a complaint, not a formulaic binary reframe (`C03`).
- `همین!` — a single voice-consistent fragment, not a dramatic fragment stack (`S01`, `C09`).
- Rhetorical questions and emoticons — preserve as personal voice unless the user explicitly requests copyediting.
