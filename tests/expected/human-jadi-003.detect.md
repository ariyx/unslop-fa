# human-jadi-003 — expected detect baseline

**Expected findings:** none.

This fixture is a chronological first-person complaint. Repetition and list framing serve narrative emphasis and accountability.

## Near misses that should remain unflagged

- `صبر کردیم و صبر کردیم.` — deliberate narrative repetition, not redundant restatement (`C01`).
- `یعنی ۲۳ دقیقه...` — clarifies a timestamp with elapsed time and therefore adds information (`C02`).
- `در نتیجه` — expresses a specific causal claim attributed to support, not a canned transition (`C05`).
- Repeated `انتظار می ره` bullets — list framing that makes separate expectations scannable, not excessive symmetry (`C04`) or robotic rhythm (`C09`).
- `نه اینکه ...` — a concrete behavioral contrast, not a rhetorical binary template (`C03`).
- Personal frustration and informal spelling — preserve as voice; do not mistake them for translationese (`P02`).
